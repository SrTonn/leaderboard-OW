// export const end = (startTime) => {
//   const endTime = performance.now();
//   let timeDiff = endTime - startTime; // in ms
//   // strip the ms
//   timeDiff /= 1000;

//   // get seconds
//   const seconds = Math.round(timeDiff);
//   console.log(`Time elapsed: ${seconds} seconds`);
// };

// export const start = () => performance.now();
